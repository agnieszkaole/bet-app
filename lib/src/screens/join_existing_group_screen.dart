import 'package:flutter/material.dart';

class JoinExistingGroupScreen extends StatelessWidget {
  const JoinExistingGroupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Dołącz do istniejącej grupy'),
      ),
    );
  }
}
